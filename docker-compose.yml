name: api-e-buteco

services:
  pg:
    image: postgres:17
    restart: always
    ports:
      - '5432:5432'
    environment:
      - POSTGRES_USER=docker
      - POSTGRES_PASSWORD=docker
      - POSTGRES_DB=db-buteco
    volumes:
        - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
  

